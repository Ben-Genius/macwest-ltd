/** Static routes for `sitemap.ts` — extend when adding dynamic segments. */
export const staticRoutes = [
  "/",
  "/about-us",
  "/board-members",
  "/guiding-principles",
  "/organizational-structure",
  "/services",
  "/projects",
  "/current-projects",
  "/past-project",
  "/partners",
  "/gallery",
  "/contact",
  "/hse-policy-statement",
] as const;
